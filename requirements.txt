# Memory Bank MCP v2 Requirements
# Enhanced AI Collaboration Memory System
# Generated: 2025-07-13.1843

# Core MCP dependencies
mcp>=1.0.0

# Database for persistent storage
aiosqlite>=0.19.0

# UUID generation for cross-project references (built-in, no install needed)
# JSON handling (built-in, no install needed)

# Date/time handling for timestamps
python-dateutil>=2.8.0

# Path handling and file operations (built-in pathlib)

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=22.0.0
